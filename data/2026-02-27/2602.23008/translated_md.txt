# 探索性記憶增強型 LLM Agent：混合在線與離線策略優化

探索仍然是使用強化學習訓練的大型語言模型 Agent 的關鍵瓶頸。雖然先前的方法利用預訓練知識，但在需要發現新狀態的環境中卻表現不佳。我們提出探索性記憶增強型在線與離線策略優化（EMPO²），一個混合 RL 框架，利用記憶進行探索，並結合在線和離線策略更新，使 LLM 在使用記憶時表現良好，同時也確保在沒有記憶情況下的魯棒性。在 ScienceWorld 和 WebShop 上，EMPO² 分別相對於 GRPO 達到了 128.6% 和 11.3% 的改進。此外，在分佈外測試中，EMPO² 展示了對新任務的優越適應性，只需少量記憶試驗且無需參數更新。這些結果突顯 EMPO² 是構建更具探索性和泛化能力的基於 LLM Agent 的有前景框架。

## 1 介紹

[FIGURE:25_graph.png] (a)

大語言模型 (LLMs) 最近已成為具有推理、規劃以及與外部環境互動能力的強大代理 (Achiam et al., 2023; Park et al., 2023; Yao et al., 2023; Kim et al., 2025)。當結合強化學習 (RL) 時，此類代理可根據經驗和反饋調整其行為，使其超越靜態提示或監督微調 (Guo et al., 2025; Tan et al., 2024)。此範例驅動了互動式決策、工具使用和具身人工智慧等領域的最近進展 (Feng et al., 2025b; Lu et al., 2025b; Feng et al., 2025a; Dong et al., 2025; Luo et al., 2025)。

然而，當前基於 LLM 的代理的一個關鍵限制在於它們依賴於利用先驗知識，而非進行系統性的探索。雖然 RL 框架強調平衡探索與利用，但許多 LLM 代理系統主要利用預訓練知識，並且只在熟悉的分佈內進行有限搜索。因此，這些代理在需要發現新狀態或主動獲取新信息的環境中往往表現不佳，而不是重複使用已知的內容。

為了應對這一挑戰，最近的研究已將外部記憶模組納入 LLMs，作為長期記憶的形式。這使模型能夠利用過去的經驗來修正失敗的嘗試，從而在後續試驗中改進決策，而無需參數更新 (Shinn et al., 2023; Zhang et al., 2023)。然而，如 Zhang et al. (2023) 所指出的，此類方法的性能往往會迅速飽和，因為使用靜態參數收集經驗無法完全捕捉連續改進所需的多樣性。

[FIGURE:concept.png] 圖 2：非參數化更新可以鼓勵探索，引導參數化更新。

在本研究中，我們提出了一個統一框架，使 LLM 代理能夠通過廣泛探索更有效地學習，方法是使用 RL 聯合更新其參數化策略參數，並通過互動更新其非參數化記憶模組。至關重要的是，非參數化更新不僅補充，而且增強了參數化學習的效率，從而實現更有效的探索和適應。此雙更新範例充當了參數級優化和記憶擴增推理之間的橋樑。雖然在學習期間利用記憶，但要實現更可概括的智慧，需要減少對外部記憶的依賴，而是將其好處直接嵌入到模型的參數中。為此，我們提出 Exploratory Memory-Augmented On- and Off-Policy Optimization (EMPO²)，一種新的混合 RL 算法，在推廣階段結合了兩種模式——取決於是否使用記憶——以及在更新階段的兩種模式——在線和離線學習——從而使代理能夠在有記憶時利用記憶，同時在沒有記憶時保持穩健。

在我們的實驗中，我們在兩個廣泛使用的多步具身推理環境上評估 EMPO²，這些環境需要探索來解決複雜任務：ScienceWorld (Wang et al., 2022) 和 WebShop (Yao et al., 2022)。我們將其性能與一系列非參數化和參數化（離線和在線）RL 方法進行比較。如圖 1 所總結的，EMPO² 大幅超越先前的算法，在 ScienceWorld 上相較強大的在線 RL 基線 GRPO 實現了 128.6% 的改進，在 WebShop 上實現了 11.3% 的改進。圖 1 (a) 中的訓練曲線進一步顯示，與收斂到次優解的 GRPO 不同，EMPO² 利用持續探索並成功解決任務。此外，針對 OOD 實驗（圖 1，最右側），模型也能以僅有的幾次試驗和無權重更新的情況下達到良好分數，表明更新後的模型已獲得使用記憶探索未見或不熟悉環境的能力。這些結果突出了 EMPO² 是構建更適應性和更可概括的具身代理的有前景的方向。

## 2 預備知識

在線 RL 包括在 rollout 階段和更新階段之間交替進行，在 rollout 階段中使用由 θ \theta 參數化的當前策略 π \pi 生成軌跡，在更新階段中根據這些 rollout 優化策略。

**策略 Rollout。** 我們考慮一個設置，其中給定從 p ​ ( 𝒰 ) p(\mathcal{U}) 採樣的任務 u u ，一個 LLM agent 通過與環境的多步交互來解決任務。從任務 u u 開始，LLM π θ \pi_{\theta} 生成第一個自然語言動作 a 1 ∼ π θ ( ⋅ ∣ u ) ∈ 𝒜 a_{1}\sim\pi_{\theta}(\cdot\mid u)\in\mathcal{A} 。執行此動作後，環境返回獎勵 r 1 r_{1} 和下一個狀態 s 1 s_{1} 。在一般時刻 t t ，以當前狀態 s t s_{t} 和任務 u u 為條件，策略產生下一個動作 a t + 1 ∼ π θ ( ⋅ ∣ s t , u ) a_{t+1}\sim\pi_{\theta}(\cdot\mid s_{t},u) 。此交互循環持續進行，直到任務完成或達到最大步數。rollout 軌跡因此定義為狀態、動作和獎勵的序列，τ = ( u , a 1 , r 1 , s 1 , a 2 , r 2 , … , s T ) . \tau=\big(u,a_{1},r_{1},s_{1},a_{2},r_{2},\ldots,s_{T}\big).

**群組相對策略優化。** 群組相對策略優化 (Group Relative Policy Optimization, GRPO) (Shao et al. , 2024 ) 通過比較同一任務 u u 的多個 rollout 來更新策略，消除了 PPO (Schulman et al. , 2017 ) 中對價值函數的需求。給定任務 u u ，策略 π θ \pi_{\theta} 生成 N N 條 rollout 軌跡 { τ ( 1 ) , … , τ ( N ) } \{\tau^{(1)},\ldots,\tau^{(N)}\} 。每條軌跡獲得回報 { R ( 1 ) , … , R ( N ) } \{R^{(1)},\ldots,R^{(N)}\} ，定義為沿著軌跡的獎勵之和：R ( i ) = ∑ t = 1 T r t ( i ) . R^{(i)}=\sum_{t=1}^{T}r_{t}^{(i)}. 。對於在軌跡 τ ( i ) \tau^{(i)} 中採取的每個動作 a t ( i ) a_{t}^{(i)} ，我們將其相對優勢定義為：A ​ ( a t ( i ) ) = R ( i ) − 1 N ​ ∑ j = 1 N R ( j ) σ ​ ( R ) , A(a_{t}^{(i)})=\frac{R^{(i)}-\frac{1}{N}\sum_{j=1}^{N}R^{(j)}}{\sigma(R)}, 其中來自獎勵高於平均值軌跡的動作獲得正優勢，而來自表現較差軌跡的動作獲得負優勢。GRPO 損失則為：

|  | 𝔼 u ∼ p ​ ( 𝒰 ) { τ ( i ) } i = 1 N ∼ π θ old \displaystyle\mathbb{E}_{\begin{subarray}{c}u\sim p(\mathcal{U})\\
\{\tau^{(i)}\}_{i=1}^{N}\sim\pi_{\theta_{\text{old}}}\end{subarray}} | [ 1 N ​ T ​ ∑ i = 1 N ∑ t = 1 T min ⁡ ( ρ θ ​ ( a t ( i ) ) ​ A ​ ( a t ( i ) ) , clip ​ ( ρ θ ​ ( a t ( i ) ) , 1 − ϵ , 1 + ϵ ) ​ A ​ ( a t ( i ) ) ) ] \displaystyle\Bigg[\frac{1}{NT}\sum_{i=1}^{N}\sum_{t=1}^{T}\min\Big(\rho_{\theta}(a_{t}^{(i)})A(a_{t}^{(i)}),\text{clip}\big(\rho_{\theta}(a_{t}^{(i)}),1-\epsilon,1+\epsilon\big)A(a_{t}^{(i)})\Big)\Bigg] |  |
| --- | --- | --- | --- |
|  |  | − β D KL ( π θ ( ⋅ | u ) ∥ π ref ( ⋅ | u ) ) , \displaystyle\quad-\beta\,D_{\text{KL}}\!\big(\pi_{\theta}(\cdot|u)\,\|\;\pi_{\text{ref}}(\cdot|u)\big), |  | (1) |

其中 ρ θ ​ ( a t ( i ) ) = π θ ​ ( a t ( i ) | s t ( i ) , u ) π θ old ​ ( a t ( i ) | s t ( i ) , u ) , \rho_{\theta}(a_{t}^{(i)})=\frac{\pi_{\theta}(a_{t}^{(i)}|s_{t}^{(i)},u)}{\pi_{\theta_{\text{old}}}(a_{t}^{(i)}|s_{t}^{(i)},u)}, 其中 β ≥ 0 \beta\geq 0 控制朝向參考策略 π ref \pi_{\text{ref}} 的正則化強度。

## 3 The Exploration Problem of LLM Agents

LLMs encode rich prior knowledge, but such priors often fail to reflect the actual rules or dynamics of a given environment. Blind reliance on these priors can lead to erroneous behaviors, making it necessary for agents to adapt through direct interaction and trial-and-error. A key requirement for such adaptation is exploration , which involves seeking information beyond pre-training, sometimes by taking atypical or counterintuitive actions. However, current LLM-based agents struggle with this (Qiao et al. , 2024 ; Zhou et al. , 2024 ) , as it demands stepping outside the distribution of behaviors where the model feels most confident.

Consequently, many prior studies have sought to align agents with new environments through warm-start supervised fine-tuning (SFT) using numerous golden trajectories (Song et al. , 2024 ; Qiao et al. , 2024 ; Xiang et al. , 2024 ) , leveraging large-scale models such as GPT-4 (Tang et al. , 2024 ; Lin et al. , 2023 ) , or employing human engineering or well-established simulation information (Choudhury and Sodhi, 2025 ) . While these methods achieve strong results in constrained settings, their effectiveness is limited to cases where such external support is available, and they generalize poorly to unseen scenarios without it.

[FIGURE:exploration_problem.png] Figure 3: When training LLM with GRPO in ScienceWorld, the agent struggles because of insufficient exploration. For instance, in the task “turn on the red light bulb,” the agent must first find the red light bulb before activating it. However, the agent fails to locate it and, as a result, cannot complete the task. Rather than analyzing the cause of failure and exploring alternative actions, the agent proceeds unchanged, so its score stagnates even as additional training steps are taken.

Therefore, we focus on how to efficiently train agents in online RL through trial and error, without any prior embedding of the environment’s rules. The key challenge is that, without intrinsic exploration capability, online RL struggles to optimize effectively. As illustrated in Figure 3 , in ScienceWorld (Wang et al. , 2022 ) environment the agent is given the mission “turn on the red light bulb.” The instructions specify that the agent should first focus on the light bulb and then build a circuit to activate it, based on the current room observation. However, since no red light bulb is present in the observation, the agent must search the environment to locate it. Instead, the agent follows the instruction literally, attempts to focus on the red light bulb, and fails because it does not exist in the room. Ideally, when an agent fails to reach its goal, it should analyze the reasons for failure and broaden its action space to discover successful strategies. Yet in representative online RL algorithms GRPO (Shao et al. , 2024 ) , prior trajectory rollouts provide no continuity beyond a scalar reward signal, thereby restricting exploration and ultimately limiting learning.

## 4 Method

In this section, we present Exploratory Memory-augmented On- and Off-Policy Optimization (EMPO 2 ), a novel algorithm aimed at tackling the exploration challenges in online RL. EMPO 2 operates in two modes for both rollout phase and update phase. During rollout, actions can be generated either through (1) prompting without memory , where no retrieved information is used, or (2) memory-augmented prompting , conditioned on tips retrieved from memory. In the update phase, rollouts with memory-augmented prompting are used in two ways: (a) on-policy , where tips are retained and the update is performed with the original prompt, and (b) off-policy , where tips are removed during update. Notably, tips are generated not by a separate model but by the policy π θ \pi_{\theta} itself, which is continually updated during training. The full algorithm is provided in Appendix A .

### 4.1 使用自生成記憶促進探索

EMPO 2 的一個關鍵組件是其使用記憶來維持跨滾動軌跡（rollouts）的連續性。從智能體的互動中獲得的信息可以通過策略優化編碼到參數中，但也可以記錄在智能體持續查閱的外部記憶中。由於我們的策略從預訓練的 LLM 初始化，該 LLM 具有固有的總結和反思能力，這些能力可以作為輔助信號利用，除了標量獎勵外，從而更有效地引導探索。為了實現這一點，EMPO 2 整合了參數化（LLM 內的參數更新）和非參數化（外部記憶）更新，強化了滾動軌跡之間的聯繫並促進探索，所有數據和指導都由智能體自主生成。

[FIGURE:motivation3.png] 圖 4：在 EMPO 2 中，當前的策略參數 $\pi_{\theta}$ 用於審查過去的滾動軌跡，所產生的見解被添加到記憶中。這個更新的記憶影響後續的滾動軌跡並促進探索。

在非參數化更新中，類似於 Reflexion（Shinn et al., 2023），智能體審查過去的滾動軌跡，生成自我指導提示，並將其存儲在記憶中。這些提示幫助智能體避免重複犯錯並探索新策略。與 Reflexion 不同，Reflexion 聚焦於迭代的語言指導以在下一次試驗中獲得更高的獎勵，而我們的方法旨在讓這些提示導致增強的探索，最終通過參數化更新來鞏固。

自生成記憶和提示。我們定義一個記憶緩衝區 $\mathcal{M}=\{\text{tip}_{1},\text{tip}_{2},\ldots\}$，其存儲由策略 $\pi_{\theta}$ 在軌跡反思期間生成的反思提示。正式地說，當任務 $u$ 的第 $i$ 個疇（episode）在時間步 $t$ 終止時，策略以最終狀態 $s_{t}$ 和提示生成提示詞（tip-generation prompt）作為輸入，並生成一個提示，其中 $\text{tip}_{i}\sim\pi_{\theta}(s_{t},u,\text{tip-generation prompt})$。下面提供了一組說明性示例，而提示生成提示詞呈現在附錄 B 中，其他示例包含在附錄 E.1 中。

### 4.2 Parameterize non-parametric updates via hybrid policy optimization

Agents can use memory to improve exploration and learning efficiency, but the acquired knowledge needs be internalized into model parameters to enhance inherent capabilities. To this end, we propose two modes for the rollout and update phases, whose combinations yield three hybrid learning modes (Figure 5 ).

[FIGURE:EMPO.png] Figure 5: EMPO 2 mode combinations. By combining the two rollout modes and update modes, three EMPO mode configurations are possible: on-policy learning without memory, on-policy learning with memory and off-policy learning.

Rollout Modes. During rollouts, the agent samples between the two modes, selecting one mode at each step: mode (2) with memory rollout probability p p and mode (1) with probability 1 − p 1-p . The ablation study of p p can be found in Appendix F.1 .

- (1) Prompting Without Memory. For each task u u , at each timestep t t , the policy π θ \pi_{\theta} generates actions conditioned only on the current state s t s_{t} and the task u u : a t + 1 ∼ π θ ( ⋅ ∣ s t , u ) . a_{t+1}\sim\pi_{\theta}(\cdot\mid s_{t},u).
- (2) Memory-Augmented Prompting. For each task u u , at each timestep t t , a retrieval operator Retr ​ ( o t ; ℳ ) ⊆ ℳ \mathrm{Retr}(o_{t};\mathcal{M})\subseteq\mathcal{M} selects tips most relevant to the current state s t s_{t} , e.g., via similarity search in the embedding space. We denote the retrieved set as tips t \text{\fcolorbox{empo2}{white}{\textcolor{empo2}{tips}}}_{t} . In memory-augmented prompting, the policy conditions its action on both s t s_{t} and tips t \text{\fcolorbox{empo2}{white}{\textcolor{empo2}{tips}}}_{t} : a t + 1 ∼ π θ ( ⋅ | s t , u , tips t ) . a_{t+1}\sim\pi_{\theta}\!\left(\cdot\,\middle|\,s_{t},u,\text{\fcolorbox{empo2}{white}{\textcolor{empo2}{tips}}}_{t}\right). We limit the number of retrieved tips at 10.

Prompting Without Memory. For each task u u , at each timestep t t , the policy π θ \pi_{\theta} generates actions conditioned only on the current state s t s_{t} and the task u u : a t + 1 ∼ π θ ( ⋅ ∣ s t , u ) . a_{t+1}\sim\pi_{\theta}(\cdot\mid s_{t},u).

Memory-Augmented Prompting. For each task u u , at each timestep t t , a retrieval operator Retr ​ ( o t ; ℳ ) ⊆ ℳ \mathrm{Retr}(o_{t};\mathcal{M})\subseteq\mathcal{M} selects tips most relevant to the current state s t s_{t} , e.g., via similarity search in the embedding space. We denote the retrieved set as tips t \text{\fcolorbox{empo2}{white}{\textcolor{empo2}{tips}}}_{t} . In memory-augmented prompting, the policy conditions its action on both s t s_{t} and tips t \text{\fcolorbox{empo2}{white}{\textcolor{empo2}{tips}}}_{t} : a t + 1 ∼ π θ ( ⋅ | s t , u , tips t ) . a_{t+1}\sim\pi_{\theta}\!\left(\cdot\,\middle|\,s_{t},u,\text{\fcolorbox{empo2}{white}{\textcolor{empo2}{tips}}}_{t}\right). We limit the number of retrieved tips at 10.

Update Modes. Trajectories generated under rollout mode (1) are directly used for updates, whereas those generated under rollout mode (2)—memory-augmented prompting—follow one of two update modes chosen at random during the update phase. Mode (b) is selected with off-policy update probability q q , and mode (a) with probability 1 − q 1-q . The ablation study of q q can be found in Appendix F.1 .

- (a) On-Policy Updates. On-policy update uses the same prompt as in the rollout, and ρ θ ​ ( a t ( i ) ) \rho_{\theta}(a_{t}^{(i)}) in eq. 1 becomes ρ θ ​ ( a t ( i ) ) = π θ ​ ( a t ( i ) ∣ s t ( i ) , u , tips t ) π θ old ​ ( a t ( i ) ∣ s t ( i ) , u , tips t ) . \rho_{\theta}(a_{t}^{(i)})=\frac{\pi_{\theta}(a_{t}^{(i)}\mid s_{t}^{(i)},u,\text{\fcolorbox{empo2}{white}{\textcolor{empo2}{tips}}}_{t})}{\pi_{\theta_{\text{old}}}(a_{t}^{(i)}\mid s_{t}^{(i)},u,\text{\fcolorbox{empo2}{white}{\textcolor{empo2}{tips}}}_{t})}.
- (b) Off-Policy Updates. In this mode, the stored log-probabilities ℓ t tips = log ⁡ π θ ​ ( a t ∣ s t , u , tips t ) \ell^{\text{tips}}_{t}=\log\pi_{\theta}(a_{t}\mid s_{t},u,\text{\fcolorbox{empo2}{white}{\textcolor{empo2}{tips}}}_{t}) are replaced with the log-probabilities assigned by the same policy π θ \pi_{\theta} when conditioned only on ( s t , u ) (s_{t},u) , namely ℓ t no-tips = log ⁡ π θ ​ ( a t ∣ s t , u ) \ell^{\text{no-tips}}_{t}=\log\pi_{\theta}(a_{t}\mid s_{t},u) . In this formulation, the advantage update is performed based on how natural the action appears under the distribution without tips. This construction can be interpreted as a form of reward-guided knowledge distillation . Trajectories sampled under the tips-conditioned policy act as teacher demonstrations, while the student policy π θ ( ⋅ ∣ s , u ) \pi_{\theta}(\cdot\mid s,u) is updated to reproduce those trajectories in proportion to their advantage. High-reward trajectories ( A ^ t > 0 \hat{A}_{t}>0 ) are reinforced, while low-reward trajectories ( A ^ t < 0 \hat{A}_{t}<0 ) are suppressed, resulting in selective distillation that emphasizes beneficial behaviors. In this way, tips serve as an intermediate scaffolding mechanism that improves exploration and trajectory quality, while the reward signal ensures that only advantageous behaviors are ultimately retained. Consequently, the final policy learns to internalize the benefits of tip conditioning without requiring tips at inference time. Appendix C provides an illustrative breakdown and a summary table for the calculation of the importance sampling ratio.

On-Policy Updates. On-policy update uses the same prompt as in the rollout, and ρ θ ​ ( a t ( i ) ) \rho_{\theta}(a_{t}^{(i)}) in eq. 1 becomes ρ θ ​ ( a t ( i ) ) = π θ ​ ( a t ( i ) ∣ s t ( i ) , u , tips t ) π θ old ​ ( a t ( i ) ∣ s t ( i ) , u , tips t ) . \rho_{\theta}(a_{t}^{(i)})=\frac{\pi_{\theta}(a_{t}^{(i)}\mid s_{t}^{(i)},u,\text{\fcolorbox{empo2}{white}{\textcolor{empo2}{tips}}}_{t})}{\pi_{\theta_{\text{old}}}(a_{t}^{(i)}\mid s_{t}^{(i)},u,\text{\fcolorbox{empo2}{white}{\textcolor{empo2}{tips}}}_{t})}.

Off-Policy Updates. In this mode, the stored log-probabilities ℓ t tips = log ⁡ π θ ​ ( a t ∣ s t , u , tips t ) \ell^{\text{tips}}_{t}=\log\pi_{\theta}(a_{t}\mid s_{t},u,\text{\fcolorbox{empo2}{white}{\textcolor{empo2}{tips}}}_{t}) are replaced with the log-probabilities assigned by the same policy π θ \pi_{\theta} when conditioned only on ( s t , u ) (s_{t},u) , namely ℓ t no-tips = log ⁡ π θ ​ ( a t ∣ s t , u ) \ell^{\text{no-tips}}_{t}=\log\pi_{\theta}(a_{t}\mid s_{t},u) . In this formulation, the advantage update is performed based on how natural the action appears under the distribution without tips.

This construction can be interpreted as a form of reward-guided knowledge distillation . Trajectories sampled under the tips-conditioned policy act as teacher demonstrations, while the student policy π θ ( ⋅ ∣ s , u ) \pi_{\theta}(\cdot\mid s,u) is updated to reproduce those trajectories in proportion to their advantage. High-reward trajectories ( A ^ t > 0 \hat{A}_{t}>0 ) are reinforced, while low-reward trajectories ( A ^ t < 0 \hat{A}_{t}<0 ) are suppressed, resulting in selective distillation that emphasizes beneficial behaviors. In this way, tips serve as an intermediate scaffolding mechanism that improves exploration and trajectory quality, while the reward signal ensures that only advantageous behaviors are ultimately retained. Consequently, the final policy learns to internalize the benefits of tip conditioning without requiring tips at inference time. Appendix C provides an illustrative breakdown and a summary table for the calculation of the importance sampling ratio.

[FIGURE:low_filtering.png] Figure 6: Masking tokens stabilizes training.

Stabilizing Off-Policy Training. Off-policy training is prone to instability and may collapse (see Figure 6 ). In such cases, gradient normalization, entropy loss, KL loss, and policy gradient loss can all diverge to NaN. Prior work, Yang et al. ( 2025 ) shows that low-probability tokens destabilize training by amplifying gradient magnitudes through unbounded likelihood ratios. Motivated by this, we introduce a masking mechanism that suppresses the advantage term for tokens whose probability under π θ \pi_{\theta} falls below a threshold δ \delta . Finally, the loss in Eq. 1 is modified as

|  | 𝔼 u ∼ p ​ ( 𝒰 ) { τ ( i ) } ∼ π θ old [ 1 N ​ T ∑ i = 1 N ∑ t = 1 T \displaystyle\mathbb{E}_{\begin{subarray}{c}u\sim p(\mathcal{U})\\
\{\tau^{(i)}\}\sim\pi_{\theta_{\text{old}}}\end{subarray}}\Bigg[\frac{1}{NT}\sum_{i=1}^{N}\sum_{t=1}^{T} | min ( ρ θ ( i , t ) A ( a t ( i ) ) , clip ( ρ θ ( i , t ) , 1 − ϵ , 1 + ϵ ) A ( a t ( i ) ) ) ⋅ 𝟏 π θ ​ ( a t ( i ) | s t ( i ) , u ) ≥ δ ] \displaystyle\min\Big(\rho_{\theta}^{(i,t)}A(a_{t}^{(i)}),\;\text{clip}\big(\rho_{\theta}^{(i,t)},1-\epsilon,1+\epsilon\big)A(a_{t}^{(i)})\Big)\cdot\mathbf{1}_{\pi_{\theta}(a_{t}^{(i)}|s_{t}^{(i)},u)\geq\delta}\Bigg] |  |
| --- | --- | --- | --- |
|  |  | − β D KL ( π θ ( ⋅ | u ) ∥ π ref ( ⋅ | u ) ) . \displaystyle\quad-\beta D_{\text{KL}}\!\big(\pi_{\theta}(\cdot|u)\,\|\;\pi_{\text{ref}}(\cdot|u)\big). |  | (2) |

[FIGURE:entropy.png] Figure 7: Policy entropy comparison with vs. without intrinsic rewards.

Intrinsic Rewards for Exploration. To further encourage exploration, and inspired by prior work on exploration-targeted online RL (Burda et al. , 2018b ; Bellemare et al. , 2016 ; Ecoffet et al. , 2019 ) , we introduce an intrinsic reward based on the novelty of the current state. A memory list stores distinct states, and for each new state we compute its cosine similarity with existing entries. If the similarity falls below a threshold, the state is added to memory and assigned a reward. The intrinsic reward is defined as r intrinsic = 1 n r_{\text{intrinsic}}=\frac{1}{n} , where n n denotes the number of similar past states. This mechanism encourages the agent to explore novel states even when no extrinsic reward is provided by the environment and maintains policy entropy, as shown in Figure 7 .

## 5 相關工作

**LLM Agents 在多步具身任務中的應用**。LLM agents 在多步具身任務中已在不同的範式下進行過研究。資料驅動的方法（Song et al.，2024；Xiong et al.，2024；Qiao et al.，2025；2024；Tajwar et al.，2025）透過有效的資料收集方法和模仿學習來增強決策能力。基於模型的 agents（Tang et al.，2024；Zhou et al.，2024）建立世界模型，通常透過使用 GPT-4 等大型閉源系統生成程式碼。其他方法（Lin et al.，2023；Choudhury and Sodhi，2025）透過模型轉移或利用模擬環境提供的特殊資訊來加強推理。相比之下，我們的方法減少了對此類外部資源的依賴，並強調透過 agent 自身的探索和自我改進來實現自主成長。

**LLM Agents 的記憶機制**。為了實現從過往經驗的漸進式改進，Reflexion（Shinn et al.，2023）和 REMEMBERER（Zhang et al.，2023）利用外部記憶。Reflexion 儲存口頭反思供後續提示使用，而 REMEMBERER 記錄觀察、行動、獎勵和 Q 值，檢索類似案例作為少樣本範例。這些方法表明 LLMs 可以在不進行參數更新的情況下進行改進。然而，由於參數固定，它們無法擴展內在知識，所以適應只是短期的（Zhang et al.，2023），依賴外部記憶而不是實現長期演進和泛化。

**透過知識蒸餾進行學習**。我們的混合非策略更新函數在線上訓練期間充當獎勵引導的知識蒸餾。Snell et al.（2022）引入了上下文蒸餾，其中模型首先使用 Teacher 提示（包含指示、範例、解釋和暫存板推理）解決任務，然後學習透過離線的、基於 SFT 的蒸餾從最小 Student 提示產生最終答案。相比之下，我們將知識蒸餾整合到線上 RL 中，利用線上適應性同時增強探索，以實現更高效的訓練。

**LLM Agents 的 RL**。RL 為透過環境交互的觀察和獎勵信號來最佳化 LLM 參數提供了健全的框架。先前的工作 Retrospex（Xiang et al.，2024）表明離線 RL（從大型記錄資料集學習最優策略）可以改進 LLM agent 的性能。最近的研究專注於線上 RL（Shao et al.，2024；Feng et al.，2025b；Wang et al.，2025），其中 agents 進行實時學習。GiGPO（Feng et al.，2025b）透過對具有相似觀察的 rollouts 進行分組來推進 GRPO，實現更細粒度的信用分配和更強的性能。我們的工作透過將非參數記憶更新整合到策略內和策略外學習中來推進這一線上 RL 方向，從而產生顯著更高的樣本效率。

**增強線上 RL 的探索**。線上 RL 中的核心挑戰是有效的探索。古典方法，如計數型探索（Bellemare et al.，2016）和隨機網路蒸餾 Random Network Distillation（Burda et al.，2018b）使用內在獎勵來鼓勵新穎性。Go-Explore（Ecoffet et al.，2019）儲存關鍵狀態並從中重新探索，解決了 Atari 遊戲等難以探索的任務。其 LLM 擴展 Intelligent Go-Explore（Lu et al.，2025a）在 TextWorld（Côté et al.，2018）等環境中取得了強勁的成果，但依賴於大型閉源模型且不執行參數更新。在我們的並行工作中，RLVMR（Zhang et al.，2025）採用暖啟動 SFT 來引發多樣化的推理類型（規劃、探索和反思），並在線上 RL 期間為每種推理類型提供密集的過程級獎勵，增強探索和信用分配。這些研究共同強調了結構化探索對於將 RL 擴展到複雜環境的重要性。

## 6 實驗

為了檢驗 EMPO 2 的有效性，我們在兩個廣泛使用的 LLM agent 基準上進行了大量實驗：ScienceWorld（Wang et al., 2022）和 WebShop（Yao et al., 2022），使用 Qwen2.5-7B-Instruct（Qwen et al., 2025）作為基礎模型。我們評估的 EMPO 2 效能是訓練模型在測試時不帶記憶的效能。

### 6.1 ScienceWorld

ScienceWorld（Wang et al., 2022）是一個互動式文字基準，其中 agent 在小學水準執行科學實驗。成功完成這些實驗需要長期的多步驟規劃、假設檢驗和結果解釋，以及充分的探索以確定必要工具的位置和應採取的適當行動。ScienceWorld 包含來自不同主題的任務，在我們的實驗中，我們涵蓋了 19 項任務，跨越化學、分類、生物學、電學和測量。

基線。我們將 EMPO 2 與幾種 RL 方法進行比較。對於非參數 RL，Reflexion（Shinn et al., 2023）以非參數方式更新記憶，透過納入來自前序軌跡的 LLM 反思，並在後續試驗的提示中使用它們。對於離線 RL，Retrospex（Xiang et al., 2024）利用 SFT 訓練的模型，並使用透過隱式 Q 學習（Implicit Q-learning）（Kostrikov et al., 2022）學習的 Q 函數動態重新評分行動。官方 Retrospex 論文使用了較小的 Flan-T5-Large（Chung et al., 2024）（770M），並納入了人工設計的啟發式方法來在評估期間協助 agent。相比之下，為了確保我們實驗設置的一致性，我們將 Retrospex 的基礎模型標準化為 Qwen2.5-7B-Instruct，並排除這些啟發式方法。最後，對於線上 RL，我們將 GRPO（Shao et al., 2024）納入作為代表性基線。更多細節在附錄 D 中提供。

P[1]¿ \arraybackslash p#1

[FIGURE:13.png] 表 1：ScienceWorld 的比較結果。ScienceWorld 中的每項任務包含多個變體。我們使用前五個變體進行訓練，並在 20 個未見過的測試變體上進行評估。粗體表示每項任務的最佳效能，而紅色陰影標記了參數更新得分低於非參數更新的情況。我們評估的 EMPO 2 效能是訓練模型在測試時不帶記憶的效能。